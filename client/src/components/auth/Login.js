import React from 'react';
import { Grid, Typography, Button, FormControl, FormHelperText, TextField } from '@mui/material';

const Login = () => {
    return (
        <Grid container>
            Hey there!
        </Grid>
    );
}

export default Login;